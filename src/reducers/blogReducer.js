export default function (state = {}, action) {
    switch(action.type) {

        case 'CREATE_POST':
            // TODO: something here
            state = Object.assign({}, state)
    }
    return state
}
